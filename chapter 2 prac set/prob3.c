#include <stdio.h>

int main()
{
    // Write a program to check whether a number is divisible by 97 or not.
    int a;
    printf("enter the number : ");
    scanf("%d",&a);
    printf("the value of a%97 is %d",a%97);
    // when answer is come 0 then it is divisible by 97.

    return 0;
}