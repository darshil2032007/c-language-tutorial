#include <stdio.h>

int main()
{
    char st[30];
    gets(st);// the entered string is stored in st!
    puts(st);// entered in new line after string is print.

    printf("%s",st);
    printf("smart");

    return 0;
}